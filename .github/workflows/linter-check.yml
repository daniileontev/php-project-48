name: difference calculator
on:
  push:
    branches:
    - main
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: run install
        run: make install
      
      - name: run linter
        run: make lint
